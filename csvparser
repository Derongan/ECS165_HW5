import csv

with open('2012_Q1.csv', newline='') as csvfile:
    row_read = csv.reader(csvfile)
    for row in row_read:
        if row == '':
            continue
        else:
            print(row)
