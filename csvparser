import csv

with open('2012_Q1.csv') as csvfile:
    row_read = csv.DictReader(csvfile)
    
    for row in row_read:
        flag = 0
        for col in row:
            if col == 'CID' or col == 'INSTRUCTOR(S)' or col == 'SEAT' or row == '':
                flag = 1
        if flag == 1:
            continue
        else:
            print(row)
